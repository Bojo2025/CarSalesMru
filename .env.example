# Optional Postgres connection string.
# Leave unset for local development — LotMoris uses in-memory PGLite.
# For production (Vercel, Railway, etc.), set a Postgres URL (Neon works well).
DATABASE_URL=
